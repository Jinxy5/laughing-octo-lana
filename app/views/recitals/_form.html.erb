<%= form_for(@recital) do |f| %>
  <% if @recital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recital.errors.count, "error") %> prohibited this recital from being saved:</h2>

      <ul>
      <% @recital.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :image_path %><br>
    <%= f.text_field :image_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
