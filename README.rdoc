== Start

sidekiq -dl log/sidekiq.log
sudo redis-server config/redis.conf
pumactl -F config/puma.rb start

== Stop

sidekiq - don't really know. need to search for the pid. Pretty damn annoying to be honest. 
redis-ctl shutdown
pumactl -F config/puma.rb start

== Debug

tail -f the following:

log/production.development
log/production.log
log/sidekiq.log

/var/log/redis/redis-server.log

-
redis-ctl monitor 